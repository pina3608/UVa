#include <vector>
#include <list>
#include <map>
#include <set>
#include <deque>
#include <queue>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <cctype>
#include <string>
#include <cstring>

#include <assert.h>

using namespace std;


#define forn(i, n) for (int i = 0; i < (int)(n); i++)
#define forit(i, a) for (__typeof((a).begin()) i = (a).begin(); i != (a).end(); i++)
#define fi(n) forn(i,n)
#define fj(n) forn(j,n)
#define fk(n) forn(k,n)
#define sz size
#define mp make_pair
#define pb push_back
#define all(v) (v).begin(), (v).end()
#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define MIN(a,b) (((a) < (b)) ? (a) : (b))
#define last(a) int(a.size() - 1)

#define union(x,y) ((x) | (y))
#define intersection(x,y) ((x) & (y))
#define set(x,n)  ((x) && (1 << (n)))
#define unser(x,n) ((x) && (~(1 << (n))))
#define isset(x,n) (((x) & (1 << (n))) != 0)

#define fs first
#define sc second


typedef long long ll;
typedef vector<int> vi;
typedef vector<vi> vii;
typedef vector<string> vs;
typedef pair<int,int> pii;
typedef pair<int,pii> piii;
typedef map<string,int> msi;


const long double eps = 1e-9;
const int inf = (1 << 30) - 1;
const ll inf64 = ((ll)1 << 62) - 1;
const long double pi = acos(-1.0);

int toInt(string s){int r=0;istringstream sin(s);sin>>r;return r;}
ll toLl(string s){int64 r=0;istringstream sin(s);sin>>r;return r;}
template<class T> string toString(T n){ostringstream ost;ost<<n;ost.flush();return ost.str();}
double toDouble(string s){double r=0;istringstream sin(s);sin>>r;return r;}//NOTES:toDouble(


int ni() { int a; scanf( "%d", &a ); return a; }
double nf() { double a; scanf( "%lf", &a ); return a; }
long long nll() { long long a; scanf( "%lld", &a ); return a; }



int main(){
	
	
	return 0;
}
